# <Epic name>

## <Story name>

As a [type of user—person or system, as appropriate], I want [goal], so that [benefit].

## Acceptance criteria

Use numbered lists only. For a single layer of behavior, one group is enough.

1. ...
2. ...

If the same story must cover both UI-facing and backend (or API) behavior, add a second group with a heading, each group still using numbered bullets only:

### UI (or equivalent)

1. ...
2. ...

### Backend (or API, or equivalent)

1. ...
2. ...

## Technical notes

Implementation hints only: concise guidance for the developer (tricky logic, new integrations, ordering constraints). Omit this section if there is nothing useful to add.

- ...

## References

Spec and application code only (no tests). Paths relative to the repository root.

- `specs/...`
- `slot_machine/...` (or other non-test source paths)

## Missing requirements

Only add this section when applicable. Use both subheadings below; leave a subheading out (or write “None.”) if it does not apply.

### Gaps versus specification

- ...

### Implementation quality

- ...

## Spec and code disagreement

Only add this section when applicable. Summarize what the phase spec expects versus what the implementation does, in plain language.

- ...
